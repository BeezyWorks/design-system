// Only used by Jest (and by nothing when an app consumes this package — the
// app's Metro/Babel transforms these sources with its own config).
module.exports = {
  presets: ['babel-preset-expo'],
}
