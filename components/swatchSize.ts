/** Edge length of every swatch tile. `ThemeSwatch` and `FontSwatch` share it
 * so the appearance and font pickers line up in a settings screen. Internal:
 * not exported from the package, so a swatch's size can't be overridden. */
export const SWATCH_SIZE = 56
