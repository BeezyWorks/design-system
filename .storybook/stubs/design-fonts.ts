// Vite can't load the font assets (`require()` of .ttf files and the
// expo-google-fonts packages), and Storybook doesn't register them anyway —
// stories fall back to system fonts. See typography/fonts.ts.
export const DesignFonts = {}
